namespace OrderSystem.Enums
{
    /// <summary>
    /// Enum for the joins
    /// </summary>
    public enum JoinType
    {
        Inner,
        Left,
        Right,
        LeftOuter,
        RightOuter,
        Cross
    }
}